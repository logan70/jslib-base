// rollup.config.aio.min.js
// umd
const { uglify } = require('rollup-plugin-uglify')

const { inputOption, outputOption } = require('./rollup.config.aio.js')
const { output } = require('../../jslib.config')

inputOption.plugins.push(uglify())

exports.inputOption = inputOption

exports.outputOption = {
  ...outputOption,
  file: output.aioMin
}